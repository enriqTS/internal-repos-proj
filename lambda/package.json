{
  "name": "lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/handler.js --external:@aws-sdk"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.500.0",
    "archiver": "^7.0.0",
    "busboy": "^1.6.0",
    "ignore": "^5.3.0",
    "shared": "*"
  },
  "devDependencies": {
    "@types/archiver": "^6.0.0",
    "@types/aws-lambda": "^8.10.130",
    "@types/busboy": "^1.5.4",
    "esbuild": "^0.21.0"
  }
}
