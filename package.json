{
  "name": "internal-repos",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "lambda",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fast-check": "^3.15.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
